Options -Indexes
RewriteEngine On

# Keep real assets and directories working normally.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Send React Router routes to the Vite entry point.
RewriteRule ^ index.html [L]
